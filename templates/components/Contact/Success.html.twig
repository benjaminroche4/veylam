{# Confirmation block shown after a successful contact submission #}
<div class="rounded-2xl border border-white/10 bg-white/5 p-8 text-center">
    <h2 class="text-xl font-semibold text-white">{{ 'contact.success.title'|trans }}</h2>
    <p class="mt-2 text-neutral-400">{{ 'contact.success.body'|trans }}</p>
</div>
